LOCAL DEVELOPMENT — run after every code change:

  cd C:\Users\serve\ansel\IMVUCompanion
  .\scripts\Run-Dev.ps1

Your test exe (only this one):

  bin\Release\net8.0-windows10.0.19041.0\IMVUCompanion.exe

Do NOT test from:
  bin\Debug\...
  publish\...
  publish.*\...
  release\...  (installers only)

When ready to ship a new version to testers:

  1) CONNECT GITHUB FIRST (one uninterrupted run):
       gh auth login
       gh auth setup-git
     Or run preflight only:
       .\scripts\Preflight-GitHub.ps1

  2) One-shot (recommended — preflight + build + push + release + gist):
       .\scripts\Ship-Release.ps1

  Manual steps instead:
       .\scripts\Publish-Release.ps1
       git add -A && git commit -m "Release vX.Y.Z" && git push
       git tag vX.Y.Z && git push origin vX.Y.Z
       .\scripts\Finish-Release.ps1

  Scopes needed: repo, gist