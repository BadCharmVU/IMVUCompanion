LOCAL DEVELOPMENT — only path that matters:

  C:\Users\serve\ansel\IMVUCompanion\bin\Release\net8.0-windows10.0.19041.0\IMVUCompanion.exe

After every code change:

  cd C:\Users\serve\ansel\IMVUCompanion
  .\scripts\Run-Dev.ps1

That script:
  - cleans publish/release-* junk
  - rebuilds into bin\Release\net8.0-windows10.0.19041.0 only
  - starts that exe

Do NOT run or test from:
  publish\  publish.*\  release*\  bin\Debug\  bin\...\win-x64\

User config (welcome messages, commands) is NOT next to the exe.
It is always:
  %LOCALAPPDATA%\IMVUCompanion\messages.json
  %LOCALAPPDATA%\IMVUCompanion\commands.json

Dev builds show "Dev build" on the update button (no false "update available").
Installer updates are only for the INSTALLED app on other PCs.

When ready to ship:

  .\scripts\Ship-Release.ps1
